model Organization {
  id          String       @id @default(dbgenerated("pg_catalog.gen_random_uuid()")) @db.Uuid
  name        String
  slug        String
  logo        String?
  createdAt   DateTime
  metadata    String?
  members     Member[]
  invitations Invitation[]

  @@unique([slug])
  @@map("organization")
}
